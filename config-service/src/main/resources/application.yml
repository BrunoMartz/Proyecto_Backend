server:
  port: 8888
  
spring:
  cloud:
    config:
      server:
        git:
          uri: https://BrunoMartz@bitbucket.org/brunomartz/config-data.git
          username: ${bit_user}
          password: ${bit_pass}
          search-paths: api
          default-label: "master"
          
  security:
    user:
      name: root
      password: adminadmin
